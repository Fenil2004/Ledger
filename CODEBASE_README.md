Repository scaffold created by GitHub Copilot assistant.

Contents:
- `backend/` — Express API prototype (in-memory) and `db.sql` schema
- `frontend/` — Vite + React minimal UI prototype

Next recommended steps:
1. Install dependencies in `backend` and `frontend` and run both to try the prototype.
2. Replace in-memory data with a real database (Postgres). Use `backend/db.sql` to create schema.
3. Add authentication (Supabase Auth or JWT) and secure API endpoints.
4. Add Tailwind CSS or upgrade the UI components.
5. Add CSV/PDF export endpoints and connect to storage.
